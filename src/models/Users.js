// 1. Guardar al usuario en la DB
// 2. Buscar a un usuario que se quiere loguear por su email
// 3. Buscar a un usuario por su ID
// 4. Editar la información de un usuario
// 5. Eliminar la información de un usuario

// Voy a hacer todo este sistema en un objeto literal que se va a llamar user, con sus diferentes métodos para crear y bla
const User = {
    fileName: '../data/usuarios.json',

    getData: function(usersData) {
        return 
    }
    // create: 
}